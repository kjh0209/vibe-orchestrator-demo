import { UserCredentials, AuthResponse } from './types';\n\nconst mockDatabase: Record<string, string> = {}; // 이메일을 키로, 비밀번호의 해시를 값으로 저장\n\nexport const authenticateUser = async (credentials: UserCredentials): Promise<AuthResponse> => {\n  const { email, password } = credentials;\n  const storedPassword = mockDatabase[email];\n\n  if (!storedPassword) {\n    return { success: false, message: 'User not found' };\n  }\n\n  if (storedPassword !== password) { // 간단한 평문 비교 (실제론 해시 비교 필요)\n    return { success: false, message: 'Invalid password' };\n  }\n\n  return { success: true, token: 'mockToken'; // 실제론 jwt 사용 };\n};